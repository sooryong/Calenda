# LoRA 어댑터 설정
r: 16
alpha: 32
dropout: 0.05
bias: none
task_type: CAUSAL_LM

# Qwen2.5 / HyperCLOVA 모두에서 사용 가능한 target_modules
# (둘 다 LLaMA-like 구조라 동일 이름)
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj

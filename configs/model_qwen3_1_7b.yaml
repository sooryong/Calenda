# Qwen3-1.7B 베이스 설정 (c4 데이터로 "배포 가능한 더 큰 모델" 천장 실험)
# 호환: Qwen3-0.6B와 동일 패밀리 — ChatML(<|im_start|>), target_modules 동일(q/k/v/o/gate/up/down_proj),
#   thinking 모델(비-thinking 고정은 0.6B와 동일 처리). 파이프라인 그대로 재사용.
# 폰 예산: Q8 ~1.8GB(초과) → 배포는 Q4~Q5(~1.0–1.3GB). 비교는 "1.7B Q4 vs 0.6B c4 Q8".
name: qwen3-1.7b
hf_id: Qwen/Qwen3-1.7B
max_seq_len: 1024
trust_remote_code: false
# response_template / supports_system 키 없음 → train_lora가 Qwen 기본값 사용
#   (response_template="<|im_start|>assistant\n", supports_system=true)

# system_prompt는 0.6B(c4)와 글자까지 동일 — 같은 조건 비교.
system_prompt: |
  당신은 메시지에서 일정 정보를 추출하는 모델입니다. 날짜·시각을 계산하지 말고 표현 그대로 추출합니다.
  date: 상대 날짜는 토큰으로(내일·모레·글피·다음주화·1주후·1개월후 등), 명시 날짜는 YYYY-MM-DD, 없으면 null.
  time: {hour, minute, marker} 객체로 추출(marker는 오전·오후·저녁·밤·낮·정오·자정 또는 null). 24시간 변환 금지.
  title에는 활동/주제만 넣고(누구와·발신자는 앱이 붙임), 소속 기관이 있으면 organizer에 넣습니다.
  지정된 JSON 스키마에 맞춰 순수 JSON만 출력하고, 명시되지 않은 정보는 null을 씁니다.
  <대화내역>이 있으면 맥락을 참고하되 추출 대상은 마지막 <메시지>이며, 여러 후보가 협의됐다면 가장 최근 합의값을 씁니다.
  schedule_status는 두 질문으로 정합니다. ① 사용자 본인이 직접 갈/할 미래의 일인가? 아니면(거래·결제·적립·배송·광고·인사·과거 회고·남의 일정) "no". ② 본인 일이면 — 확정이면 "yes", 미확정(안내·초대·제안·신청·마감·미수락)이면 "pending". 날짜·시각·금액이 붙어 있어도 본인 일이 아니면 "no", 날짜가 없어도 본인 행동이면 "pending".
